"""Example simulations and visualisations for the modular simulator."""

# The directory is treated as a package so modules such as
# ``examples.simple_irreversible_system.dashboard`` can be executed via
# ``python -m``.  Keeping the examples importable avoids the need for ad-hoc
# ``sys.path`` manipulation when launching helper scripts from within the
# repository tree.

__all__ = []
